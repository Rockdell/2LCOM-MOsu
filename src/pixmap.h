
/** @file pixmap.h */

/** @defgroup pixmap Pixmap
 *  @{
 *
 *  Sprites used in the game.
 */

/** Sprite for number 1. */
extern char *um[];

/** Sprite for number 2. */
extern char *dois[];

/** Sprite for number 3. */
extern char *tres[];

/** Sprite for MOsu's logo. */
extern char *mosu[];

/** Sprite for play button. */
extern char *play[];

/** Sprite for exit button. */
extern char *exit_menu[];

/** Sprite for easy mode button. */
extern char *easy[];

/** Sprite for hard mode button. */
extern char *hard[];

/** Sprite for wrong timing. */
extern char *wrong[];

/** Sprite for correct timing. */
extern char *correct[];

/** Sprite for cursor. */
extern char *cursor[];

/** Sprite for letter Q. */
extern char *letterQ[];

/** Sprite for letter W. */
extern char *letterW[];

/** Sprite for letter E. */
extern char *letterE[];

/** Sprite for letter R. */
extern char *letterR[];

/** @} end of pixmap */
